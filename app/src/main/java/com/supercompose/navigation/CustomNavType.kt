package com.tutorial.supercompose.navigation

/*
class AssetParamType : NavType<ComposeTheme>(isNullableAllowed = true) {
    override fun get(bundle: Bundle, key: String): ComposeTheme? {
        return bundle.getParcelable(key)
    }

    override fun parseValue(value: String): ComposeTheme {
        return Json.decodeFromString<ComposeTheme>(value)
    }

    override fun put(bundle: Bundle, key: String, value: ComposeTheme) {
        bundle.putSerializable(key, value)
    }
}*/
